name: Test fallback none option
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          tool: shellcheck
          fallback: 'none'
      - name: Assert shellcheck is installed without fallback
        run: |
          echo "Checking shellcheck installation with fallback=none..."
          if ! command -v shellcheck > /dev/null 2>&1; then
            echo "FAIL: shellcheck was not found in PATH"
            exit 1
          fi
          SC_VERSION=$(shellcheck --version | head -2 | tail -1)
          echo "actual: $SC_VERSION"
          echo "PASS: shellcheck installed successfully with fallback=none: $SC_VERSION"
